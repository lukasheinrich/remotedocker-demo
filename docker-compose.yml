version: '2'
services:
  webserver:
    command: npm start
    image: lukasheinrich/remotedocker-webterm
    environment:
      - REMOTEDOCKER_BROKERSERVER=broker:4000
      - REMOTEDOCKER_SOCKETHOST=broker
    ports:
      - 3000:3000
  broker:
    image: lukasheinrich/remotedocker
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: python webserver.py
    ports:
      - 4000:4000
      - 5000-5099:5000-5099
